-- Generated from generator/demo_specs/aws-malaysia-oil-gas-trading.json
-- Regenerate with: python3 generator/gen_repo_docs.py aws-malaysia-oil-gas-trading
-- This is the schema that is actually deployed for MY_OIL_GAS_TRADING.

CREATE OR REPLACE TABLE RAW.MARKET_REPORTS AS
SELECT
  'MAR-' || LPAD(SEQ4()::VARCHAR, 4, '0') AS ID,
  CASE MOD(SEQ4(), 5) WHEN 0 THEN 'Selangor' WHEN 1 THEN 'Johor' WHEN 2 THEN 'Penang' WHEN 3 THEN 'Sabah' WHEN 4 THEN 'Sarawak' END || ' Market Report ' || (SEQ4() + 1) AS NAME,
  CASE MOD(SEQ4(), 5) WHEN 0 THEN 'Selangor' WHEN 1 THEN 'Johor' WHEN 2 THEN 'Penang' WHEN 3 THEN 'Sabah' WHEN 4 THEN 'Sarawak' END AS REGION,
  CASE MOD(SEQ4(), 4) WHEN 0 THEN 'Crude Cargo' WHEN 1 THEN 'Refined Product' WHEN 2 THEN 'LNG' WHEN 3 THEN 'Paper Hedge' END AS CATEGORY,
  CASE WHEN MOD(SEQ4(), 17) = 0 THEN 'Inactive' ELSE 'Active' END AS STATUS,
  DATEADD('day', -MOD(SEQ4() * 37, 900), CURRENT_DATE()) AS CREATED_AT,
  ROUND(GREATEST(196800000.000000, LEAST(283200000.000000, NORMAL(240000000.000000, 14400000.000000, RANDOM(45937)))), 4) AS KPI_TRADING_REVENUE,
  ROUND(GREATEST(1.550000, LEAST(10.850000, NORMAL(6.200000, 1.550000, RANDOM(45938)))), 4) AS KPI_CARGO_MOVEMENTS,
  ROUND(GREATEST(34727000.000000, LEAST(49973000.000000, NORMAL(42350000.000000, 2541000.000000, RANDOM(45939)))), 4) AS KPI_OPEN_POSITIONS
FROM TABLE(GENERATOR(ROWCOUNT => 20));

-- Equi-join on a precomputed entity index. A correlated scalar subquery per
-- row would be evaluated 250,000 times.
CREATE OR REPLACE TABLE RAW.PRICE_FEEDS AS
WITH ents AS (
  SELECT ID, CATEGORY, STATUS, ROW_NUMBER() OVER (ORDER BY ID) - 1 AS RN
  FROM RAW.MARKET_REPORTS
),
gen AS (
  SELECT SEQ4() AS I FROM TABLE(GENERATOR(ROWCOUNT => 250000))
)
SELECT
  'EVT-' || LPAD(g.I::VARCHAR, 9, '0') AS EVENT_ID,
  e.ID AS ENTITY_ID,
  DATEADD('day', -MOD(g.I, 90), CURRENT_DATE()) AS EVENT_DATE,
  e.CATEGORY,
  e.STATUS,
  ROUND(GREATEST(67.240000, LEAST(96.760000, (NORMAL(82.000000, 4.920000, RANDOM(46337)) + (((FLOOR(e.RN / 5) - 1.5) * 0.55 + (MOD(e.RN, 5) - 2) * 0.35)) * 4.920000))), 4) AS KPI_AVG_BRENT,
  ROUND(GREATEST(1.968000, LEAST(2.832000, (NORMAL(2.400000, 0.144000, RANDOM(46338)) + (((FLOOR(e.RN / 5) - 1.5) * 0.55 + (MOD(e.RN, 5) - 2) * 0.35)) * 0.144000))), 4) AS KPI_TAPIS_PREMIUM,
  ROUND(GREATEST(0.688800, LEAST(0.991200, (NORMAL(0.840000, 0.050400, RANDOM(46339)) + (((FLOOR(e.RN / 5) - 1.5) * 0.55 + (MOD(e.RN, 5) - 2) * 0.35)) * 0.050400))), 4) AS KPI_STORAGE_CONTANGO
FROM gen g
JOIN ents e ON e.RN = MOD(g.I, 20);
